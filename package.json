{
  "name": "claw-club-nft",
  "version": "0.1.0",
  "description": "Claw Club NFT - 500 unique NFTs on Solana",
  "license": "MIT",
  "scripts": {
    "build": "anchor build",
    "test": "anchor test",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "deploy:mainnet": "anchor deploy --provider.cluster mainnet"
  },
  "devDependencies": {
    "@anchor-lang/cli": "^0.30.0",
    "@solana/cli-config": "^1.18",
    "@solana/web3.js": "^1.88",
    "chai": "^4.3.10"
  }
}
